version: "3.6"

services:
 sut:
  build: ./test
  user: root
  volumes:
    - "./:/root/.minetest/games/super_sam/"
    - "worlds:/root/.minetest/worlds/"
  ports:
    - "30000:30000/udp"

volumes:
  worlds: {}